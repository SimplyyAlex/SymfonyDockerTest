{% extends 'base.html.twig' %}

{% block body %}
    <div class="w-screen h-screen flex items-center justify-center">
        <div class="w-100 h-100 bg-gray-800 rounded-lg p-4">
            <form method="POST" action="{{ path('delete') }}" class="flex flex-col gap-2">
                <label for="id">ID:</label>
                <input id="id" type="text" class="w-full h-10 bg-red-800 rounded-lg p-1">
            </form>
        </div>
    </div>
{% endblock %}
