{% extends 'base.html.twig' %}

{% block body %}
    <div class="w-screen h-screen flex items-center justify-center bg-gray-200">
        <div class="w-full max-w-sm bg-gray-800 rounded-2xl p-6 shadow-xl">
            <form method="POST" action="{{ path('delete_request') }}" class="flex flex-col gap-4">
                <label for="id" class="text-sm font-medium text-gray-300">Insert person ID:</label>
                <input
                    id="id"
                    name="id"
                    type="text"
                    class="w-full h-11 px-4 text-gray-900 rounded-lg bg-gray-200 focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
                >
                <button
                    type="submit"
                    class="w-full h-11 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg transition"
                >
                    Delete
                </button>
            </form>
        </div>
    </div>
    {% if app.request.get('status') %}
        {% if app.request.get('status') == 'success' %}
            <div class="fixed bottom-0 inset-x-0 p-4 text-green-800 border-t border-green-300 bg-green-50 text-center z-50">
                {{ app.request.get('message') }}
            </div>
        {% elseif app.request.get('status') == 'error'  %}
            <div class="fixed bottom-0 inset-x-0 p-4 text-red-800 border-t border-red-300 bg-red-50 text-center z-50">
                {{ app.request.get('message') }}
            </div>
        {% endif %}
    {% endif %}
{% endblock %}
