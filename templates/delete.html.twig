{% extends 'base.html.twig' %}

{% block body %}
    <div class="w-screen h-screen flex items-center justify-center">
        <div class="w-100 h-100 bg-gray-800 rounded-lg p-4 shadow-2xl">
            <form method="POST" action="{{ path('delete_request') }}" class="flex flex-col gap-2">
                <label for="id" class="text-sm text-semibold text-gray-500">ID:</label>
                <input id="id" name="id" type="text" class="w-full h-10 bg-gray-300 rounded-lg p-1">
                <button type="submit" class="w-full h-10 bg-red-500 rounded-lg p-1 cursor-pointer">Smazat</button>
            </form>
        </div>
    </div>
{% endblock %}
