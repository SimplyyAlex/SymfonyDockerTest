{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="form-person">
            <h1>Add Person</h1>
            <form action="{{ path('index') }}" method="POST">
                <label for="name">Name</label><br>
                <input class="form-input" type="text" id="name" name="name" required><br>

                <label for="surname">Surname</label><br>
                <input class="form-input" type="text" id="surname" name="surname" required><br>

                <input class="form-submit" type="submit" name="submit" value="Submit">
            </form>
        </div>
        <div class="people-container">
            <table class="people-table">
                <thead>
                <tr>
                    <th>Person's ID</th>
                    <th>Name</th>
                    <th>Surname</th>
                </tr>
                </thead>
                <tbody>
                {% for person in people %}
                    <tr>
                        <td><strong>{{ person.getId() }}</strong></td>
                        <td>{{ person.getName() }}</td>
                        <td>{{ person.getSurname() }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
